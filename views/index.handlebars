
<h1>Hamburger Hut</h1>

<main>

    {{#each burgers}} {{#unless this.devoured}}
    <div>
        <form id={{this.id}} >{{this.burger}}
            <button class="devour" type="submit" data-id={{this.id}}> Devour! </button>
        </form>
    </div>
    {{/unless}} {{/each}}

    <h2>Eaten: </h2>

    {{#each burgers}} {{#if this.devoured}}

        <div>
            {{this.burger}}
        </div>

    {{/if}} {{/each}}

    </div>
    </div>
</main>

<h2>Make A Custom Burger</h2>

<div class="row">
    <div class="col s12">
        <form action="/burgers" method="POST">
            <input type="text" id="newBurger" name="newBurger">
            <button type="submit" class="addBurgerButton">Add The Burger</button>
        </form>
    </div>
</div>
{{!-- 
<script>
    $(function() {
  $(".devour").on("click", function(event) {
    var id = $(this).data("id");
    var devoured = true;
    var newDevour = {
      devoured: devoured
    };
    console.log(id);
    console.log(newDevour);
        // Send the PUT request.
    $.ajax("/burgers/devour/" + id, {
      type: "PUT",
      data: newDevour
    }).then(
      function() {
        // Reload the page to get the updated list
        console.log("something happened");
        debugger;
        location.reload();
      }
    );
  });
    }); --}}

{{!-- </script> --}}